StylesPath = .vale/styles
MinAlertLevel = suggestion
Vocab = OrgoniteTerms

[formats]
md = markdown

# Apply to all markdown content articles
[src/content/articles/*.md]
BasedOnStyles = Vale, OrgoniteGuide

# Ignore frontmatter and code blocks
TokenIgnores = (\-\-\-[\s\S]*?\-\-\-), (`[^`]+`)
